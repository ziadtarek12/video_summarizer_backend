# LLM Provider Configuration
# Choose provider: "google" or "openrouter"
LLM_PROVIDER=google

# Google AI Studio (Gemini) - get key from https://aistudio.google.com/
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_MODEL=gemini-1.5-flash

# OpenRouter - get key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=meta-llama/llama-3.1-70b-instruct:free

# LLM Settings
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.7

# Available LLM Models (comma-separated list for frontend dropdown)
# Google models start with 'gemini', OpenRouter models use full path format
# Add models you want users to select from
AVAILABLE_LLM_MODELS=gemini-1.5-flash,gemini-1.5-pro,gemini-2.0-flash,meta-llama/llama-3.1-70b-instruct:free,openai/gpt-4o-mini,anthropic/claude-3-haiku

# Whisper Configuration (using faster-whisper, large-v3 enforced at code level)
# Language: ar (Arabic) or en (English) only
WHISPER_LANGUAGE=ar
WHISPER_DEVICE=auto

# Output Configuration
OUTPUT_DIR=./output
DEFAULT_NUM_CLIPS=5

# Proxy Configuration (Optional)
# Use if YouTube blocks the server IP. Format: http://user:pass@host:port
YOUTUBE_PROXY=

# Google Apps Script Bridge (Optional)
# Deploy your own GAS script to validate YouTube videos before downloading
# This helps bypass age-gating and provides video title validation
# Format: https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
GAS_BRIDGE_URL=
